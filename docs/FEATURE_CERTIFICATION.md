# Lotview Feature Certification

No feature counts as working because the code exists.

A feature only counts as working when CI, staging, logs, tests, and a real user flow prove it.

## Certification Levels

| Status | Meaning |
|---|---|
| Not Started | Code may or may not exist. No proof. |
| Code Exists | Feature has code but no verified proof. Not production-ready. |
| CI Verified | Lint, typecheck, build, and tests pass in CI. |
| Staging Verified | Feature works in staging with realistic dealer data. |
| Observable | Logs, errors, metrics, and recovery path exist. |
| User-Flow Verified | A real user can complete the workflow end-to-end. |
| Production Ready | All proof is complete. |

## Required Proof

Every production feature must have CI proof, automated tests, staging proof, logs or monitoring, real user-flow proof, failure or recovery behavior, an owner, and a last verified date.

Recent partial proof: tenant and super-admin user mutation routes, modular Facebook page/account/queue routes, FB inbox thread routes, FB Marketplace account routes, manager notification email verification routes, legacy Facebook posting account/template/queue/OAuth/page/vehicle/manual-post routes, super-admin onboarding run and launch-checklist item routes, manager competitor alert routes, manager appraisal get/update/delete routes, remarketing vehicle update/delete routes, PBS config/webhook-event management routes, call analysis recording/criteria routes, call scoring template/criteria/sheet/response routes, call participant read routes, automation sequence/queue routes, re-engagement campaign routes, automation analytics execution routes, contact activity update routes, automation sequence execution/message update routes, CRM contact/tag management, task, activity, message-template, and message-history routes, Marketplace Blast ad-template and vehicle/photo routes, public cached vehicle-image routes, and AI payment vehicle routes now use strict positive ID parsing before tenant-scoped storage reads or writes. Competitor alert, PBS management, automation queue, automation analytics, contact activity, inactive-contact, CRM contact/task, Marketplace Blast queue, Marketplace photo download, cached public vehicle-image index, and AI payment credit-score numeric filters also fail closed instead of partially parsing malformed values. This improves RBAC, inventory, and tenant-boundary proof but does not certify full cross-tenant isolation.

Additional partial proof: manager autopost queue photo-override and dequeue routes validate UUID queue-item route parameters before dealership-scoped queue mutations reach storage. This improves RBAC, tenant-boundary, and Facebook posting safety, but does not certify marketplace posting or cross-tenant isolation.

Additional partial proof: appointment read/mutation/follow-up routes and notification read-marker routes validate UUID route parameters before appointment, follow-up, or notification storage/service calls. This improves lead workflow RBAC and tenant-boundary proof, but appointment scheduling and notification flows still need real service implementation, staging, observability, and user-flow proof.

Additional partial proof: appointment owner query filters and notification/email-outbox limit query filters now use strict positive-integer parsing instead of permissive `parseInt`, so partially numeric values fail closed before tenant-scoped reads. This improves route contract proof but does not certify appointment or notification user flows.

Additional partial proof: FB inbox thread/message/audit read filters now use strict positive/non-negative integer parsing for `limit`, `offset`, and audit `threadId` before tenant-scoped message and audit reads. This improves Facebook inbox route-contract proof but does not certify Facebook inbox automation or real user flows.

Additional partial proof: modular vehicle list `page` and `limit` filters now use strict positive-integer parsing instead of permissive `parseInt`, so malformed pagination fails closed before dealership-scoped inventory reads. This improves inventory route-contract proof but does not certify full inventory CRUD, UI, staging, source-truth, or rollback proof.

Additional partial proof: modular vehicle batch AI-description `vehicleIds` now use strict positive-integer parsing before dealership-scoped vehicle reads and inventory writes. Malformed batch IDs fail closed instead of being partially parsed. This improves inventory and AI route-contract proof but does not certify batch AI description quality, staging, observability, or rollback proof.

Additional partial proof: scrape validation now normalizes VINs through the full format/check-digit validator before counting valid VINs or detecting duplicates. This prevents case/whitespace VIN variants from bypassing duplicate detection, but does not certify scraper pagination, live source-truth reconciliation, staging, observability, or rollback proof.

Additional partial proof: vehicle deduplication now canonicalizes existing inventory VINs through the full validator before matching scraped vehicles, finding duplicate groups, or merging selected duplicates. This reduces legacy VIN formatting drift during scrape storage, but does not certify full inventory CRUD, live source-truth reconciliation, staging, observability, or rollback proof.

Additional partial proof: scrape validation now rejects unsafe scraped source URLs and unsafe/non-HTTP photo URLs before a scrape result can be marked valid, and unsafe photo URLs no longer count toward photo coverage. This improves scrape intake safety but does not certify pagination, quarantine/admin review, live source-truth reconciliation, staging, observability, or rollback proof.

Additional partial proof: deployment templates and guides no longer present a hard-coded Render example host as live proof, and production gates now reject stale Render proof URLs, unsupported 10/10 production-readiness claims, missing Render Dockerfile references, and deployment docs that point operators at the non-proof verification workflow instead of the active CI Render Staging Proof job. This improves production-readiness honesty but does not certify Render staging until the CI Render Staging Proof job runs green against the configured service.

## Feature Certification Matrix

| Feature | Status | CI Proof | Tests | Staging Proof | Logs/Monitoring | Real User Flow | Owner | Notes |
|---|---|---|---|---|---|---|---|---|
| Login/Auth | Code Exists | No | Partial | No | Partial | No | TBD | Frontend login-to-inventory flow now posts credentials to `/api/auth/login`, stores the returned JWT in session storage, sends it as a Bearer token to `/api/auth/me` and `/api/vehicles`, and clears it on logout. Verify backend JWT, failed login, password reset, protected routes, rate limits, staging, observability, and rollback before production. |
| RBAC | Code Exists | Partial | Partial | No | No | No | engineering | Explicit SaaS permission matrix, permission/capability guard tests, tenant and super-admin user management plus dealership/settings route contracts, modular and legacy vehicle inventory-write route contracts, modular and legacy vehicle AI/scrape/Carfax/action route contracts, modular and legacy full inventory view read guards, dealership website-url, branding, and VDP footer tenant route contracts, financing/fee settings billing read/write route contracts with strict setting IDs and immutable ownership-field stripping, GHL/API-key/webhook-secret integration route contracts, scrape-source and manual scraper diagnostic integrations read/write route contracts with strict legacy scrape-source IDs and ownership-field stripping, email integration write and notification feed/audit read/write route contracts, call scoring route contracts with scoring response same-template storage scoping, call analysis route contracts with call analysis ownership-field stripping, and call participant read/write route contracts, CRM lead/message route contracts with CRM update ownership-field stripping and tag-assignment tenant verification, appointment/follow-up lead route contracts, automation runtime and sequence-definition lead/message route contracts with automation update ownership-field stripping, automation Facebook lead trigger auth contracts, modular and legacy Facebook posting message read/write route contracts with strict modular page/account/queue IDs and strict legacy account/template/queue IDs, FB inbox message/AI-control route contracts with strict thread IDs, legacy conversation message read/write route contracts, scheduled-message route contracts, messenger management/AI-control route contracts, Marketplace Blast account/template/queue/AI-generation/photo route contracts, manager VIN/pricing inventory-write route contracts, manager competitor alert route contracts with strict alert/filter IDs, manager autopost queue read/write route contracts, manager notification email/outbox route contracts with strict target user IDs, external integration token route contracts with strict token IDs, modular admin route contracts including active super-admin dealership strict identifier parsing, whitelisted dealership updates, duplicate tenant identity checks, lifecycle audit logging, and master-user password-hash response stripping plus active super-admin user listing through the explicit super-admin storage API, modular super-admin user creation role/dealership validation, target user strict ID parsing, audit logging, and password-hash response stripping, super-admin scrape provider route contracts, super-admin impersonation, restart, ops dashboard, onboarding and launch checklist route contracts with strict run/item IDs, super-admin secret/API-key route contracts, super-admin Facebook Catalog and Facebook Marketplace route contracts with strict account IDs, settings ownership-field stripping, and read-join tenant scoping, super-admin route guard regression coverage, super-admin filter group route contracts, and super-admin vehicle image upload route contracts exist. Full route migration, sensitive action audit proof, staging proof, and user-flow proof remain required. |
| Tenant Isolation | Code Exists | Partial | Partial | No | No | No | engineering | Tenant context boundary tests cover header spoofing, legacy tokens, explicit super-admin tenant selection, owner guard behavior, vehicle route tenant-context fail-closed behavior, strict modular vehicle-id parsing, and strict legacy vehicle-id parsing, dealership-scoped delete miss handling, vehicle view analytics dealership scoping, public tenant configuration endpoints requiring dealership context, public GHL CTA/chat handoff/auto-sync routes requiring dealership context before CRM writes, cached public vehicle image reads filtering by vehicleId, imageIndex, and dealershipId, legacy vehicle detail/Carfax/view routes requiring dealership context and strict vehicle-id parsing before storage, legacy vehicle list/detail view counts using tenant-scoped storage instead of fabricated random values, vehicle write schemas stripping client-supplied dealership IDs, scoped vehicle service-helper reads/writes, dedup merge update/delete dealership scoping, scrape run update dealership scoping, scrape queue read/write dealership scoping, Browserless scrape sourceId lookup/update dealership scoping, Facebook page provider-id lookup/update token boundaries and strict modular/legacy Facebook page IDs, modular Facebook account and manual queue routes use strict IDs before scoped storage, modular Facebook create/update payload ownership stripping, scoped Facebook storage update ownership-field stripping, legacy Facebook posting account/template/queue strict IDs, legacy vehicle mutation dealership context, dealership website-url, branding, and VDP footer context, financing/fee settings dealership context with strict setting IDs and route/storage ownership-field stripping, scrape-source and manual scraper sync dealership context with strict legacy scrape-source IDs and route/storage ownership-field stripping, email integration and notification feed/audit dealership context, call scoring template/sheet/response dealership context with response criterion same-template storage verification, call analysis dealership context with route/storage ownership-field stripping, call participant parent-record dealership context, AI runtime/settings and admin prompt template dealership context with strict positive tenant-id parsing for super-admin prompt selectors, CRM lead/message route dealership context with route/storage ownership-field stripping and tag-assignment tenant verification, appointment/follow-up route dealership context, automation runtime and sequence-definition route dealership context with strict positive tenant-id parsing and route/storage ownership-field stripping, automation Facebook lead trigger authenticated dealership context, GHL contact/appointment/opportunity outbound mutation payload ownership-field stripping, FB inbox route dealership context with strict thread IDs, Marketplace Blast dealership context, public tenancy resolution, all remaining legacy dealership route parameters including super-admin dealership detail/update, integration/API-key, Facebook Catalog, launch checklist, and super-admin Facebook Marketplace surfaces with strict account IDs, super-admin scraper logs/users/filter groups, strict super-admin filter-group and scrape-source IDs, Browserless/robust scrape provider selectors, PBS webhook bindings, and active modular super-admin dealership lifecycle routes using strict positive dealership IDs, tenant identity collision checks, whitelisted update fields, audit logging, and password-hash response stripping, active modular super-admin user creation requiring an explicit existing dealership before writing non-super-admin users, super-admin Facebook Marketplace settings ownership-field stripping and FB Marketplace read-join tenant scoping, salesperson-owned marketplace account delete/auth/verify strict account IDs before dealership-scoped account ownership checks, manager inventory autocomplete/settings/branding, inventory analysis/market-pricing, appraisal, remarketing, PBS management, PBS sales, PBS service, and PBS parts route dealership context, external PBS webhook explicit dealership binding plus required timestamped HMAC secret verification, manager VIN/pricing dealership context, manager autopost queue dealership context, manager notification email strict target-user dealership context, external integration token explicit dealership selection with strict token IDs, super-admin vehicle image upload dealership-scoped selection/update, and legacy conversation/scheduled-message/messenger-management route dealership context with strict conversation/message IDs. Full route/storage cross-tenant tests remain required. |
| Health Endpoint | CI Verified | Yes | Yes | No | Partial | Partial | engineering | Modular and legacy health, readiness, metrics, and version endpoint contracts exist. Live Render and staging proof still required. |
| Frontend Boot | CI Verified | Yes | Yes | No | No | No | engineering | Frontend now renders a tested operations dashboard surface instead of a title-only shell, supports a JWT login/logout session path, fetches health/readiness/auth/inventory APIs on load, sends the stored session token as a Bearer token, and fails closed without showing demo inventory when backend, authenticated session, or tenant context is unavailable. Authenticated Playwright and staging proof remain required. |
| Frontend Product UX | CI Verified | Yes | Yes | No | No | No | engineering | Operations dashboard, backend health/readiness/auth/inventory API loading, JWT login/logout session handling, Bearer-token API calls, live inventory rendering from `/api/vehicles` only after `/api/auth/me` returns an authenticated dealership session, fail-closed inventory blocking when session or dealership context is missing, lead workflow blocking, scrape proof state, and tenant/readiness signals have automated frontend tests. This is not yet a Playwright-verified or staging-proven product UX. |
| Vehicle Inventory CRUD | Code Exists | No | Partial | No | Partial | No | engineering | Modular vehicle tenant/read/write route contracts with strict vehicle-id parsing, legacy vehicle route id parsing contracts, dealership-scoped delete miss handling tests, vehicle view analytics tenant-boundary tests, legacy vehicle public read/view route dealership-context and strict id parsing tests, public cached vehicle image route dealership-context and image ownership tests, manager inventory autocomplete and inventory analysis/market-pricing reads requiring dealership context, scoped vehicle service-helper tests, legacy vehicle mutation inventory-write route contracts, super-admin filter group inventory read/write route contracts with strict filter-group ID parsing, super-admin vehicle image upload inventory-write/tenant update contracts, scraped VIN active-storage guard tests, manual/external vehicle write VIN guard tests including non-string VIN payload rejection, vehicle write schemas that strip client-supplied dealership IDs and normalized stock identities, server-derived stock normalization/conflict tests, active identity index migration tests, external vehicle import fail-closed dedup tests, external delete-by-VIN and destructive bulk sync full-VIN-validation contracts, manager appraisal lookup and market-pricing auto-save full-VIN-validation contracts, manual photo preservation merge tests, duplicate merge scoped update/delete tests, invalid duplicate fact promotion tests, and dedup storage-schema mapping tests exist. Invalid scraped VINs are skipped before insert/merge, invalid scraped source facts and source URLs are skipped before create/merge, invalid manually supplied VINs are rejected before duplicate checks or storage writes, vehicle creates use resolved dealership context instead of request-body dealership IDs, manual create/update paths reject same-dealership VIN conflicts, stock-number identity is derived server-side for manual writes and dedup storage, active same-dealership stock conflicts are rejected on manual create/update, scrape ingestion skips stock conflicts tied to a different active VIN, scrape ingestion can merge a stock match that lacks VIN identity, and the database migration fails closed before adding partial unique indexes if duplicate active VIN or stock identities already exist in a dealership. External imports do not fall back to direct create/update when deduplication is disabled or fails, wrong-tenant/no-match vehicle deletes do not report success, external delete-by-VIN, destructive bulk sync, and manager appraisal VIN lookups and market-pricing auto-saves reject any VIN that fails full format/check-digit validation before lookup/delete/write decisions, legacy public detail/Carfax/view routes require dealership context before storage, legacy list/detail view counts use tenant-scoped storage instead of fabricated random values, cached public vehicle images are read through vehicleId plus dealershipId plus imageIndex, vehicle views are inserted only after proving the vehicle belongs to the request dealership, photo guard and AI posting service vehicle reads/writes require dealership scope, duplicate merge writes/removals carry dealership scope, duplicate merge cleanup does not promote invalid price/odometer facts into the kept record, scraped facts map onto vehicle schema fields such as odometer/images/stock/color/VDP URL, incomplete new scrape records are skipped instead of inserted, scraped photos cannot drop manually curated photos, and unsafe/non-http scraped image URLs are filtered before active inventory create/merge/duplicate promotion. Still needs full migration execution proof, complete CRUD behavior tests, UI flow proof, staging proof, and broader source-truth overwrite protection. |
| VIN Decode | CI Verified | Yes | Yes | No | Partial | No | engineering | VIN format/check-digit validation, model-year-aware NHTSA decode calls, tenant-scoped cache behavior including mismatched cache payload bypass, NHTSA response VIN identity checks, NHTSA 50-record batch boundaries, generated-CARFAX-link labeling, provider disagreement metadata, manager VIN enrichment route wiring to the production decoder, manual/external vehicle write VIN guard behavior including non-string payload rejection, external delete-by-VIN/destructive bulk sync VIN validation, manager appraisal VIN lookup validation, manager market-pricing auto-save VIN validation, and scrape validation fail-closed behavior for any invalid scraped VIN, missing scraped identity/price facts, invalid scraped identity values, non-positive/non-numeric scraped price facts, or invalid scraped mileage/odometer facts now have automated tests. Live provider staging proof, operational rate-limit monitoring, rollback proof, and real user-flow proof remain required. |
| Scraper | Code Exists | Partial | Partial | No | Partial | No | engineering | Olympic Hyundai extraction normalizes relative image URLs, filters non-http image URLs, strictly parses JSON-LD model-year/price/MSRP/mileage facts, omits partial model-year strings and zero data-attribute prices instead of emitting them as source facts, no longer fabricates missing year/make/model in tested fixtures, extracted Olympic Hyundai facts map into the dedup storage contract, and scraper storage integration failures are returned as errors instead of hidden console-only warnings. Generic scrape entrypoints reject unconfigured scrape calls instead of returning empty/null success-shaped results. Browserless scrape persistence now writes through vehicle deduplication with the source dealership boundary, dedup skips are counted as skipped rather than fake updates, and sourceId lookups/status updates carry dealership scope when a dealership context is provided. Enhanced scrape pipeline tests prove disabled validation or disabled vehicle deduplication fail closed without active inventory writes. Scrape validation now fails closed and sends failure/quality alerts when any scraped VIN fails full format/check-digit validation, even if the overall VIN-validity ratio is otherwise above the old threshold, and it rejects scraped vehicles missing year/make/model identity facts, carrying invalid year/make/model identity values, missing required price facts, carrying non-positive/non-numeric price facts, or carrying invalid mileage/odometer facts before the scrape can be treated as valid. Shared dedup storage tests now prove invalid scraped VINs, invalid source facts, and invalid source URLs are skipped before insert/merge, incomplete new scrape records do not become active inventory, scrape merge updates/deletes carry dealership scope, scrape run completion/failure updates carry the recorded dealership boundary, scrape queue reads/writes carry dealership scope, scrape merges cannot drop manually curated inventory photos, and unsafe/non-http scraped image URLs are filtered before active inventory writes. Manual single-vehicle scrape diagnostics, manual scraper sync, and test badge routes require auth, explicit integration permissions, and dealership context; external scrape trigger routes are sensitive-limited and manual sync uses the resolved dealership instead of a body-selected dealership id. Dealership scrape-source updates strip immutable ownership fields at both the route and storage layers. Super-admin Browserless, ZenRows, Zyte, Apify, and robust scrape provider status/test/trigger routes require explicit permissions, and Browserless/robust scrape provider dealership/source selectors and super-admin scrape-source IDs use strict positive parsing. Prove pagination, quarantine tables/admin review, live source-truth reconciliation, and staging flow before launch. |
| Source Truth Reconciliation | Code Exists | No | Partial | No | Partial | No | TBD | Protect manually verified data. |
| Worker/Schedulers | Code Exists | No | No | No | Partial | No | TBD | Prove no duplicate jobs. |
| Facebook Posting | Code Exists | Partial | Partial | No | No | No | engineering | Modular and legacy Facebook Pages routes now require integrations read/write RBAC, including strict modular and legacy page IDs, priority vehicle reads/writes, tenant-scoped provider page lookups, tenant-scoped token updates, and disconnect miss handling; modular and legacy Facebook posting account/template/queue/schedule/OAuth/manual-post/config routes require messages read/write permissions and dealership context before storage; modular account update/delete and manual queue post routes reject malformed IDs before scoped storage, and legacy OAuth, page test-post, page vehicle-post, and manual queue posting routes use strict internal account/page/vehicle/queue ID parsing before tenant-scoped storage reads or writes; modular page/account/template/queue create/update payloads strip client-supplied id, userId, dealershipId, createdAt, and updatedAt fields before storage, and scoped Facebook storage updates strip immutable ownership fields as defense in depth; account/template/queue/AI-generation/manual-post/photo, manager autopost queue, super-admin Facebook Catalog config/test/sync, and super-admin Marketplace settings/account/queue/listing/activity/process surfaces require explicit permissions and dealership context; super-admin Marketplace account delete/auth/verify routes use strict account IDs, auth/verify/process routes are sensitive-limited, and salesperson-owned marketplace account delete/auth/verify routes use strict account IDs before dealership-scoped account ownership checks. Marketplace posting still must run in draft/review mode first. |
| GHL Sync | Code Exists | Partial | Partial | No | No | No | engineering | GHL/API-key integration routes require integrations read/write RBAC and dealership context, including webhook-secret reads/regeneration and test email sends; public CTA send, chat handoff, and chat lead auto-sync routes fail closed without dealership context before CRM writes; GHL mutation routes are restricted to master users until write flows are certified, and outbound contact, appointment, and opportunity mutation payloads strip internal tenant ownership fields before calling GHL. Still prove OAuth, token refresh, webhook verification, sync logs, duplicate-contact prevention, staging, observability, rollback, and real user flow. |
| AI Lead Response | Code Exists | Partial | Partial | No | No | No | engineering | AI prompt management routes require explicit ai.configure RBAC, strict prompt IDs, and dealership context where dealership-scoped, including the legacy admin prompt template write path. AI runtime generation routes require explicit ai.use RBAC, AI settings and FB inbox auto-send controls require ai.configure RBAC, and public chat fails closed without dealership context. Must still pass guardrail, inventory-grounding, prompt-injection, escalation, human approval, observability, staging, and real user-flow proof. |
| Admin Dashboard | Code Exists | No | No | No | Partial | No | TBD | Test permissions and audit logs. |
| Billing/Plans | Not Started | No | No | No | No | No | TBD | Needed before self-serve SaaS. |
| Render Deploy | Not Verified | Partial | Partial | No | Partial | No | engineering | CI now has a gated Render staging proof job that verifies health, readiness, and deployed commit when `RENDER_STAGING_ENABLED=true` and `RENDER_STAGING_BASE_URL` are configured. Render Docker build filters include `package-lock.json` so lockfile-only dependency changes trigger rebuilds. Current live Render proof remains absent until that job runs green against staging. |

## Launch Rule

No real dealership users until all critical features are CI verified, staging verified, observable, and user-flow verified.
