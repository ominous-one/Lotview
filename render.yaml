databases:
  - name: lotview-db
    plan: basic-1gb
    region: oregon
    databaseName: lotview
    user: lotview

services:
  - type: web
    name: lotview
    branch: main
    autoDeployTrigger: checksPass
    plan: standard
    runtime: node
    region: oregon
    numInstances: 1
    buildCommand: npm ci --include=dev && npm run build && npx drizzle-kit push
    startCommand: npm run start
    healthCheckPath: /ready
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: lotview-db
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
      - key: LOTVIEW_SCHEDULER_PROCESS
        value: web
      - key: LOTVIEW_ENABLE_SCHEDULERS
        value: "false"
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: PORT
        value: "5000"
      - key: SESSION_SECRET
        generateValue: true
      - key: JWT_SECRET
        generateValue: true
      - key: EXTENSION_HMAC_SECRET
        generateValue: true

  - type: worker
    name: lotview-worker
    branch: main
    autoDeployTrigger: checksPass
    plan: standard
    runtime: node
    region: oregon
    numInstances: 1
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm run start:worker
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: lotview-db
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
      - key: LOTVIEW_SCHEDULER_PROCESS
        value: worker
      - key: LOTVIEW_ENABLE_SCHEDULERS
        value: "true"
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: SESSION_SECRET
        generateValue: true
      - key: JWT_SECRET
        generateValue: true
      - key: EXTENSION_HMAC_SECRET
        generateValue: true
